# Operator-private CHIMERA peer transit overrides.
# Product defaults must stay empty: no real domains, no broad suffixes, no
# release-baked route decisions.
# One domain per line, comments start with '#'.
