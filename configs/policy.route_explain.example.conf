# Safe sample policy for route-explain docs and local process checks.
# This file is not the runtime default and is never auto-loaded by the product.

example-transit = suffix:example.org => transit
default-direct = default => direct
