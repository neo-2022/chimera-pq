# Runtime policy placeholder.
# Real domain/CIDR/protocol rules must be supplied by private operator config,
# adaptive state, or tests. The product repository must not bake live websites
# or default-transit behavior into the runtime policy.
