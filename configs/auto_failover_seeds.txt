# CHIMERA split auto-failover seed domains.
# Used by: scripts/chimera-control.sh (site-auto-bootstrap / site-auto-watch)
#
# Rules:
# - one domain per line
# - comments start with '#'
# - product defaults must stay empty; use operator-private config for real
#   domains.
