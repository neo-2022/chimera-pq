# Legacy compatibility fixture only.
# Not a CHIMERA/WEAVE MVP product example and not a release config.
carrier.profile = tls
gateway.listen_addr = 0.0.0.0:443

rekey.max_age_seconds = 300
rekey.max_packets_per_key = 10000
