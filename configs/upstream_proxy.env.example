# Legacy compatibility placeholder only.
# CHIMERA/WEAVE is not a proxy/upstream product. New installs and release
# bundles use configs/mesh_bootstrap.env.example instead.
#
# This file intentionally contains no endpoints, accounts, passwords, public
# discovery URLs, or transport defaults.
