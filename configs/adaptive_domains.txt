# Operator-private adaptive domain state.
# Product defaults must stay empty: no real domains and no release-baked
# route decisions. Runtime discovery/adaptation may populate a private state
# file outside product defaults.
