# CHIMERA-PQ client config (MVP example)
carrier.profile = tls
carrier.addr = 203.0.113.10:443
carrier.server_name = gateway.example.org

capture.mode = auto
capture.tun_supported = true

rekey.max_age_seconds = 300
rekey.max_packets_per_key = 10000
